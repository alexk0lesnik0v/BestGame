using System;
using Guns;
using UnityEngine;

namespace Inventories
{
    public class BarabanOfBullets : MonoBehaviour
    {
        private void Awake()
        {
            this.gameObject.SetActive(false);
        }
    }
}